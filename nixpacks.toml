[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python3 -m venv /app/venv",
  "/app/venv/bin/pip install -r backend/requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "/app/venv/bin/uvicorn server:app --host 0.0.0.0 --port $PORT --app-dir backend"
